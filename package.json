{
  "name": "plain",
  "version": "0.1.0",
  "description": "A Git-native CMS: the repository is the database, static files are the API, and AI is the admin.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "node --test tests/"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
